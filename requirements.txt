# Phase 1–3: Python
mediapipe>=0.10.14
opencv-python-headless>=4.8.0
numpy>=1.24.0
torch>=2.1.0
torchvision>=0.16.0
Pillow>=10.0.0
tqdm>=4.66.0

# Phase 3: export / conversion helpers
onnx>=1.15.0
onnxsim>=0.4.33
onnxruntime>=1.16.0

# Phase 4: RKNN NPU (install on Orange Pi 5 / RK3588 only)
# rknn-toolkit-lite2  # aarch64 (on-device inference)
# rknn-toolkit2       # x86 host (model conversion)
